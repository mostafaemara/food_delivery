import 'package:flutter/material.dart';

class PaymentSubmitted extends StatelessWidget {
  const PaymentSubmitted({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text("Payment Submittion Succes"),
    ));
  }
}
