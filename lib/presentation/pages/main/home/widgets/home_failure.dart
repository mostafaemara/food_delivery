import 'package:flutter/material.dart';

class HomeFailure extends StatelessWidget {
  const HomeFailure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Somthing Went Wrong"),
    );
  }
}
