import 'package:flutter/material.dart';

class OrdersLoaded extends StatelessWidget {
  const OrdersLoaded({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Loaded"),
    );
  }
}
